<h3>Editing Book</h3>
<%= form_for(@post) do |f| %>
	<h1>Title</h1>
	<%= f.text_field :title %>
	<h1>Body</h1>
	<%= f.text_area :body %>
	<%= f.submit 'Update Book' %>
	<%= link_to "show", post_path(@post.id) %>
 	<%= link_to "back", posts_path, class: 'back' %>
<% end %>
